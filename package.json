{
  "name": "cortex-bci-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "High-performance Brain-Computer Interface engine and real-time telemetry runtime",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build crates/neuro-dsp --target web --out-dir ../../packages/core/src/wasm",
    "build:packages": "tsc -p packages/core/tsconfig.json && tsc -p packages/simulator/tsconfig.json && tsc -p packages/hardware/tsconfig.json",
    "build": "npm run build:packages && npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/web",
    "test:rust": "cargo test --manifest-path crates/neuro-dsp/Cargo.toml",
    "test:pipeline": "npm run build:packages && node --test packages/core/test/pipeline.test.mjs",
    "test": "npm run test:rust && npm run test:pipeline",
    "capture:screenshots": "node scripts/capture-screenshots.mjs"
  },
  "keywords": [
    "bci",
    "eeg",
    "brain-computer-interface",
    "rust",
    "webassembly",
    "dsp",
    "neuroscience"
  ],
  "author": "Gabriel Rondon",
  "license": "Apache-2.0",
  "devDependencies": {
    "puppeteer-core": "^25.11.0"
  }
}
